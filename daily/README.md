# Daily Notes

Daily notes are for high-frequency discoveries.

Write rough findings here first. When a finding becomes reusable, promote it to `docs/en/` or `examples/`.

Do not include secrets, real domains, real IP addresses, personal agent names, channel IDs, or raw logs.

Use `daily/ja/` for Japanese operation notes.
